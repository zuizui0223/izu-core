# Prospective improvement: integrity and response-rule attribution

Approved task scope: repair two replay-integrity defects and separately investigate threshold, target choice and response damping without replacing frozen results. Parent HEAD c788c740.

1. Reject changes to all BASE values and pin source content, accepting LF/CRLF checkout equivalence only. Corrected source names alone must not bypass identity checks. Keep historical receipts/designs unchanged; document exact additional code identities for integrity-only maintenance.
2. Add an eight-cell factorial: threshold on/off; best partner/encounter-weighted centroid; constant alpha/alpha*(1-service). Add fixed-state descriptive control. Reuse original biological configuration, 21 starts, 96 histories, six frozen seeds and k={1,2,4,8,16}. Reuse paired histories across rules. No natural time calibration or fitness claim.
3. Record S/C/I fractions and absolute per-cell SS, plus separate mainland/island terminal-trait displacement, within-history spread across starts, and initial–terminal covariance slope. Do not identify S with trait retention.
4. Compute within-seed/k factorial contrasts (on minus off, centroid minus best, damping on minus off), conditional on the other two factors. Report all cells; no selective winner or new universal success threshold. Fixed is descriptive only. A no-difference result is retained.
5. Tests must first fail for identity bypass, configuration drift, and missing factorial implementation; frozen/smooth parity and paired-contrast logic then pass. Lock implementation and design hashes before full execution. Record result and provenance separately from old headline receipts. Do not tune after outcome inspection.
